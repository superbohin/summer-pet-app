# CloudBase 接入验证状态（2026-09-12）

当前为待联调版本。静态页面已上传至腾讯云测试域名，不代表已经切换生产同步。
用户随后确认希望继续使用 GitHub 域名：保留原 GitHub Pages 作为 PWA 入口，不要求
购买域名，也不把 `github.io` 当作可自行管理 DNS 的自定义域名绑定到腾讯云。

## 已完成

- 腾讯云 CLI 授权成功，确认用户的上海环境只有 PostgreSQL、没有文档库。
- 安装三张空家庭表及原子提交 RPC；真实数据库事务回滚验证通过，未留下测试记录。
- 部署 `summer-pet-family`，健康检查成功；绑定原 GitHub 家庭的根设备标识。
- 客户端登录、失败后显式重试、PG 存储层和安全校验实现完成。
- PG 服务端改为运行角色临时凭据 + 官方 `ExecutePGSql`，显式以 `service_role`
  调用三个固定 RPC；不再使用缺少 PG role 的 `app.rdb()` 管理 JWT，不新增长期密钥。
  参数十六进制编码；每次请求重新读取临时凭据；写入不自动重试；签名调试开启时拒绝执行。
- 99 项测试、代码检查、应用类型检查、普通构建和 Pages 构建通过。
- 已通过仅更新代码的命令部署修复；真实管理端调用 `health` 成功，`getConfig` 正确返回
  “Family config has not been initialized”，原 `DATABASE_42501` 权限错误已消失。
  临时只读诊断入口已移除且线上验证返回未知动作。SQL、CAM、环境变量均未改动。
- 排障后再次只读统计，本应用三张云端表仍各为 0 条；没有写入测试家庭、兑换或流水。
- 没有导入、删除或修改原家庭的打卡、金币、兑换及同步历史。
- 用户明确批准三项环境配置后，已启用匿名登录、应用家庭函数 OPA 规则；原有登录
  方式和 11 条安全来源保持不变。规则仅允许该函数 POST 的真实用户会话，拒绝
  无登录及仅公开 Key 的请求；保存 OPA 会停用旧版网关鉴权。

## 未完成：不得标记上线

1. **安全来源受套餐阻塞**：用户已批准，实际添加 `superbohin.github.io` 时腾讯云
   `CreateAuthDomain` 返回“当前套餐无法执行此操作”。回读确认未添加成功，原有
   11 条来源保留。环境显示“体验版 / NORMAL”；未购买、续费或升级任何套餐。
2. **网关正向验收尚未通过**：规则已应用并回读匹配。真实负测结果：无凭证 401；
   仅公开 Key 403 且命中本应用拒绝规则；编码函数名和双斜线也命中拒绝；末尾斜线
   及编码斜线返回 403；旧 `/web` 链路返回 `INVALID_ACCESS_TOKEN`，均未调用成功。
   但匿名登录取得的真实会话调用健康接口仍为 403 `EXCEED_AUTHORITY`，不是本应用
   deny 的拒绝信息；显式允许真实会话后仍如此。最新分流检查确认：腾讯静态来源的
   CORS 已被允许但仍报该码；Pages 来源为 403 空响应、无 CORS 许可，两者分开处理。
   临时增加精确路径 POST 的更严格诊断 deny 后，真实会话仍返回同码且未见标识；
   已恢复原策略并回读确认，不据此单独断言策略绕过。脱敏复现与请求编号见
   [平台排查材料](CLOUDBASE_SUPPORT_REQUEST.md)。不得宣称客户端连通或设备限制完成验收。
3. **前端生产切换**：静态文件已上传至腾讯云测试域名，详见下节；尚未设置
   GitHub Actions 的 CloudBase 仓库变量或合并上线。本地 GitHub CLI 未登录；
   SSH 源码访问不能替代变量设置授权。
4. **历史迁移**：必须先从原设备导出 JSON，再由原根家长设备在原 Pages 网址执行
   迁移；迁移前不要清除浏览器数据、删除家庭配置或在其它网址重建家庭。
5. **依赖审计**：此前函数依赖安装的 npm audit 报告 6 项告警（4 high / 2 moderate），
   包括保留的传统文档库 SDK 依赖链。PG 当前不加载该传统 SDK，但不能把构建/测试
   通过等同于依赖安全告警已清零；后续发布前需单独评估、修复并回归。

## 静态托管测试发布（2026-09-12）

- 地址：https://pet-d8gvpbx0b32ce11a3-1301337111.tcloudbaseapp.com/
- 用户明确要求上传后，将 `npm run build:pages` 的产物上传至已有静态托管根目录。
  跳过仅供 GitHub Pages 使用的 `.nojekyll`，39 个网站文件上传及 CLI 远端校验通过。
  逐个 HTTPS 读取这 39 个文件，均返回 200，SHA-256 与本地发布产物完全一致。
- 未使用删除或清理选项；未覆盖原有 `__auth/` 认证页面和 `cloud-admin/` 管理页。
  未更改套餐、域名、云函数、数据库或 GitHub 生产版本。
- 补充静态入口 CSP 的 `https://*.tcloudbasegateway.com` 连接许可，并增加回归测试。
  仍保留脚本同源限制、禁止对象嵌入及家庭设备签名校验。
- 当前默认域名的普通 HTTPS GET 响应带有 `Content-Disposition: attachment`；浏览器导航
  两次超时且未显示应用。因此本轮只确认上传与文件完整性，不宣称浏览器交互、
  PWA 安装或离线使用已在该域名验收通过。
  [官方默认域名说明](https://docs.cloudbase.net/service/alias)区分浏览器导航的风险提示
  中间页与其它请求的附件响应，不能仅凭普通 GET 推断所有浏览器都必定下载。
- 新域名与旧 GitHub Pages 地址的浏览器本地存储彼此隔离。没有迁移或清除旧数据；
  不要因为新地址显示空白初始状态就重新建立家庭或删除旧网址数据。

后续同类上传命令（仅更新静态文件，不删除远端其它内容）：

```sh
CLOUDBASE_CI=1 work/cloudbase-tools/node_modules/.bin/tcb hosting deploy pages-dist \
  -e pet-d8gvpbx0b32ce11a3 --ignore .nojekyll --verify \
  --entry index.html,sw.js,version.json --concurrency 4 --json
```

CLI 3.8.1 不要显式传 `/` 作为上传路径：该版本会把本地校验键加上前导斜线，
与实际存储键不一致而误报缺失；省略路径即可部署到根目录并正确校验。

继续部署前应再次读取实时环境设置，避免覆盖用户在控制台完成的配置；特别是服务端
密钥不得打印、提交到仓库、注入 `VITE_*` 或写入前端离线缓存。
已确认 CLI 3.8.1 的 `fn deploy --force` 默认覆盖云端环境变量；配置服务端 Key 后
普通更新必须改用仅更新代码的命令，或安全合并保留云端变量后再更新配置。

本轮访问检查命令：

```sh
CLOUDBASE_STATIC_ORIGIN=https://pet-d8gvpbx0b32ce11a3-1301337111.tcloudbaseapp.com \
  node --env-file=.env.local scripts/check-cloudbase-access.mjs
```

它仅创建匿名登录会话并请求健康接口，不读取或写入家庭业务表。只要正向调用未通过，
就返回非零退出码；不能把所有请求都被拒绝当成配置成功。输出不包含 Key 或会话令牌。
