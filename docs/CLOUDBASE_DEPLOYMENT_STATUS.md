# CloudBase 接入验证状态（2026-09-12）

当前为待联调版本，不代表已经切换生产同步。

## 已完成

- 腾讯云 CLI 授权成功，确认用户的上海环境只有 PostgreSQL、没有文档库。
- 安装三张空家庭表及原子提交 RPC；真实数据库事务回滚验证通过，未留下测试记录。
- 部署 `summer-pet-family`，健康检查成功；绑定原 GitHub 家庭的根设备标识。
- 客户端登录、失败后显式重试、PG 存储层和安全校验实现完成。
- 云函数 SDK 固定为 3.8.2，包含官方 API Key 鉴权及多实例凭证隔离修复；前端版本未变。
- 86 项测试、代码检查、应用类型检查、普通构建和 Pages 构建通过。
- 没有导入、删除或修改原家庭的打卡、金币、兑换及同步历史。

## 未完成：不得标记上线

1. **服务端 PG 权限**：真实 `getConfig` 返回 `DATABASE_42501`，固定分类为
   `function-permission`。临时只输出类型的诊断确认 SDK 取得的管理凭证
   `is_system_admin=true`、没有 PG `role` 字段；函数未配置 `CLOUDBASE_APIKEY`。
   临时诊断已从本地源码和已部署函数移除。没有创建额外的身份诊断 SQL 函数。
   下一步配置仅供服务端使用的 `service_role` 凭证，保持客户端默认拒绝。
   SDK 3.8.2 的假凭据离线检查确认可正确携带 API Key；这不替代真实读库验收。
2. **匿名登录与安全来源**：尚未启用匿名登录，也未追加 Pages 域名。
   已请求用户明确批准，尚待答复。
3. **网关策略**：模板已准备但尚未应用。保存会停用旧版网关鉴权，已单独请求用户
   明确批准。仍需验证仅公开 Key 被拒、真正登录会话允许。
4. **前端发布**：尚未设置 GitHub Actions 的 CloudBase 仓库变量或合并上线。
   本地 GitHub CLI 未登录；SSH 源码访问不能替代变量设置授权。
5. **历史迁移**：必须先从原设备导出 JSON，再由原根家长设备在原 Pages 网址执行
   迁移；迁移前不要清除浏览器数据、删除家庭配置或在其它网址重建家庭。
6. **依赖审计**：函数依赖安装的 npm audit 仍报告 6 项告警（4 high / 2 moderate），
   包括保留的传统文档库 SDK 依赖链。PG 当前不加载该传统 SDK，但不能把构建/测试
   通过等同于依赖安全告警已清零；后续发布前需单独评估、修复并回归。

继续部署前应再次读取实时环境设置，避免覆盖用户在控制台完成的配置；特别是服务端
密钥不得打印、提交到仓库、注入 `VITE_*` 或写入前端离线缓存。
已确认 CLI 3.8.1 的 `fn deploy --force` 默认覆盖云端环境变量；配置服务端 Key 后
普通更新必须改用仅更新代码的命令，或安全合并保留云端变量后再更新配置。
